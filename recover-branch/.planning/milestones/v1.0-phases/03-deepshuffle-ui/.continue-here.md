---
phase: 03-deepshuffle-ui
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-03-15T14:27:19.134Z
---

<current_state>
Phase 3 planning is fully complete. All 4 PLAN.md files exist and passed verification (all 8
dimensions). No execution has started yet. Next action is /gsd:execute-phase 3.

Session work: completed discuss-phase 3 (resumed from mid-session checkpoint), wrote 03-CONTEXT.md,
ran /gsd:plan-phase 3 --research, got 03-RESEARCH.md, 03-VALIDATION.md, and 4 PLAN.md files.
Checker verified all plans passed. All committed to master.
</current_state>

<completed_work>
- discuss-phase 3 resumed and completed: all 4 gray areas resolved
- 03-CONTEXT.md written and committed
- 03-RESEARCH.md written by gsd-phase-researcher (HIGH confidence, NDK headers verified)
- 03-VALIDATION.md written from template
- 03-01-PLAN.md — Qt6 build env (Wave 0, 3 tasks)
- 03-02-PLAN.md — ShuffleMatrixKnob.h + ShuffleMatrixWidget.h interface contracts (Wave 1, 2 tasks)
- 03-03-PLAN.md — ShuffleMatrixKnob.cpp + ShuffleMatrixWidget.cpp implementations (Wave 2, 2 tasks)
- 03-04-PLAN.md — DeepCShuffle.cpp rewrite + UAT checkpoint (Wave 3, 2 tasks)
- gsd-plan-checker: VERIFICATION PASSED (all 8 dimensions)
- All docs committed to master
</completed_work>

<remaining_work>
- Execute Phase 3: /gsd:execute-phase 3
  - Wave 0 (03-01): Install qt6-base-dev, update CMakeLists.txt with Qt6 find_package + AUTOMOC, reconfigure cmake, verify build passes
  - Wave 1 (03-02): Write ShuffleMatrixKnob.h and ShuffleMatrixWidget.h headers
  - Wave 2 (03-03): Implement ShuffleMatrixKnob.cpp and ShuffleMatrixWidget.cpp
  - Wave 3 (03-04): Rewrite DeepCShuffle.cpp, then UAT checkpoint in Nuke
</remaining_work>

<decisions_made>
- Single Op input (not two) — both ChannelSet pickers select from same input stream
- C++ custom DD::Image::Knob subclass with embedded Qt widget for visual matrix grid
- Matrix state serialized via to_script/from_script using "outChanName:srcChanName" string format
- Passthrough: channels not selected in in1 or in2 pass through unchanged
- No DAG port label changes — SHUF-02 satisfied by in-panel row/column labels only
- KNOB_CHANGED_ALWAYS flag required on both ChannelSet knobs
- knob_changed() must handle Knob::showPanel to populate matrix columns on panel open
- Wave 0 must install qt6-base-dev (Debian Bookworm) — Qt 6.4 headers vs Nuke's Qt 6.5.3 runtime is acceptable minor mismatch for QWidget usage
</decisions_made>

<blockers>
- None hard. Wave 0 apt install must succeed — if qt6-base-dev is unavailable, Qt6 headers
  must be located another way. Research noted this as LOW confidence (apt cache was empty at
  research time) but Debian Bookworm normally carries qt6-base-dev.
</blockers>

<context>
The phase is fully planned and ready to execute. The key implementation challenge is the
ShuffleMatrixKnob C++ custom knob — NDK examples AddCustomQt.cpp and CryptomatteLayerKnob.cpp
in /usr/local/Nuke16.0v6/Documentation/NDKExamples/examples/ are the primary templates.
The plan is interface-first: headers in Wave 1, implementations in Wave 2, Op wiring in Wave 3.
</context>

<next_action>
/clear then /gsd:execute-phase 3 — start with Wave 0 (Qt6 build environment setup).
</next_action>
