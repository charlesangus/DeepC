---
phase: 03-deepshuffle-ui
plan: 02
subsystem: ui
tags: [nuke-ndk, qt6, knob, custom-widget, deepcshuffle, cpp-headers]

# Dependency graph
requires:
  - phase: 03-deepshuffle-ui plan 01
    provides: Qt6 find_package + AUTOMOC + DeepCShuffle Qt6 link in CMakeLists.txt

provides:
  - ShuffleMatrixKnob.h — full DD::Image::Knob subclass interface (6 virtual overrides, setValue, matrixState, setChannelSets, ChannelSet accessors)
  - ShuffleMatrixWidget.h — QWidget subclass with Q_OBJECT, static WidgetCallback, syncFromKnob, onCellToggled slot, buildLayout/clearLayout private helpers
  - Serialization format locked: comma-separated "outputChannelName:sourceChannelName" pairs
  - AUTOMOC verified: moc_ShuffleMatrixWidget.cpp generated by Qt 6.5.3

affects:
  - 03-03 (ShuffleMatrixKnob.cpp implementation — includes both headers)
  - 03-04 (ShuffleMatrixWidget.cpp implementation — includes both headers)

# Tech tracking
tech-stack:
  added:
    - Qt6 6.5.3 (installed at /opt/Qt/6.5.3/gcc_64; CMAKE_PREFIX_PATH set in CMakeLists.txt)
    - CMAKE_AUTOMOC ON (triggers moc on Q_OBJECT-containing headers)
  patterns:
    - "DD::Image::Knob subclass pattern: forward-declare widget, no circular include, WidgetPointer (not DD::Image::WidgetPointer) return type"
    - "Qt widget declaration pattern: Q_OBJECT macro, forward-declare knob, include DDImage/Knob.h for CallbackReason type safety"
    - "Serialization format: comma-separated outChanName:srcChanName pairs (channel-name-indexed, not slot-indexed)"

key-files:
  created:
    - src/ShuffleMatrixKnob.h
    - src/ShuffleMatrixWidget.h
  modified:
    - src/CMakeLists.txt (target_sources ShuffleMatrixWidget.h added to Qt6 block)

key-decisions:
  - "WidgetPointer is a global typedef (not DD::Image::WidgetPointer) — Knob.h forward-declares QWidget and defines WidgetPointer at global scope"
  - "ShuffleMatrixKnob.h forward-declares ShuffleMatrixWidget (no include) to prevent circular dependency; widget.h does the reverse"
  - "Qt6 at /opt/Qt/6.5.3/gcc_64 used via list(APPEND CMAKE_PREFIX_PATH ...) before find_package — HINTS approach did not resolve Qt6_FOUND correctly"

patterns-established:
  - "Pattern: Global WidgetPointer typedef — use WidgetPointer (not DD::Image::WidgetPointer) in make_widget() override return type"
  - "Pattern: Circular dep avoidance — each header forward-declares the other; .cpp files include both"
  - "Pattern: AUTOMOC header registration — target_sources(DeepCShuffle PRIVATE ShuffleMatrixWidget.h) required so moc sees Q_OBJECT"

requirements-completed: [SHUF-01, SHUF-02, SHUF-03]

# Metrics
duration: 9min
completed: 2026-03-15
---

# Phase 3 Plan 02: ShuffleMatrixKnob and ShuffleMatrixWidget Header Declarations Summary

**C++ interface contracts for ShuffleMatrixKnob (DD::Image::Knob subclass) and ShuffleMatrixWidget (Q_OBJECT Qt widget) — moc-verified headers locking the class boundaries for Plans 03 and 04**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-03-15T14:34:20Z
- **Completed:** 2026-03-15T14:43:23Z
- **Tasks:** 2
- **Files modified:** 3 (ShuffleMatrixKnob.h created, ShuffleMatrixWidget.h created, CMakeLists.txt modified)

## Accomplishments

- `ShuffleMatrixKnob.h` declares the complete Knob subclass interface: constructor (CustomKnob2 signature), all 6 required virtual overrides (`Class`, `not_default`, `to_script`, `from_script`, `store`, `make_widget`), public accessors (`setValue`, `matrixState`, `setChannelSets`, `in1/in2ChannelSet`), and private state members with serialization format comment
- `ShuffleMatrixWidget.h` declares the `QWidget` subclass with `Q_OBJECT`, static `WidgetCallback`, public `syncFromKnob()`, slot `onCellToggled()`, private `buildLayout()`/`clearLayout()`, and all private members — including grid layout structure documentation
- AUTOMOC confirmed: Qt 6.5.3 moc processed `ShuffleMatrixWidget.h` and generated `moc_ShuffleMatrixWidget.cpp`; build passes cleanly

## Task Commits

Each task was committed atomically:

1. **Task 1: Write ShuffleMatrixKnob.h** - `4ef58a7` (feat)
2. **Task 2: Write ShuffleMatrixWidget.h** - `bcfe276` (feat)

**Plan metadata:** (docs commit to follow)

## Files Created/Modified

- `src/ShuffleMatrixKnob.h` — Knob subclass declaration with full virtual interface and ChannelSet accessors
- `src/ShuffleMatrixWidget.h` — Qt QWidget subclass with Q_OBJECT macro; AUTOMOC processes it to generate moc file
- `src/CMakeLists.txt` — Added `target_sources(DeepCShuffle PRIVATE ShuffleMatrixWidget.h)` inside `if (Qt6_FOUND)` block

## Decisions Made

- `WidgetPointer` is a **global typedef** (declared at file scope in `Knob.h` as `typedef QWidget* WidgetPointer`), not in the `DD::Image` namespace — using `DD::Image::WidgetPointer` caused a compile error; corrected to bare `WidgetPointer`
- Circular include prevention: `ShuffleMatrixKnob.h` forward-declares `ShuffleMatrixWidget`; `ShuffleMatrixWidget.h` forward-declares `ShuffleMatrixKnob` — `.cpp` implementation files include both
- Qt6 discovery uses `list(APPEND CMAKE_PREFIX_PATH "/opt/Qt/6.5.3/gcc_64")` before `find_package(Qt6 ...)` — the `HINTS` keyword approach did not set `Qt6_FOUND` reliably

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Qt6 dev prereq (Plan 03-01) not yet applied to CMakeLists.txt**
- **Found during:** Pre-task environment check
- **Issue:** The "wip: 03-deepshuffle-ui paused post-plan" git commit included CMakeLists.txt Qt6 changes (`a2dc62b`) from Plan 03-01 execution — those were already committed. The cmake configure with `HINTS` approach was found not to resolve `Qt6_FOUND`; changed to `list(APPEND CMAKE_PREFIX_PATH)` pattern which works
- **Fix:** Verified `/opt/Qt/6.5.3/gcc_64` has full Qt6 6.5.3 dev headers + moc; confirmed `Qt6_FOUND` is true by checking cmake cache; `target_sources(DeepCShuffle PRIVATE ShuffleMatrixWidget.h)` added in Task 2
- **Files modified:** src/CMakeLists.txt (CMAKE_PREFIX_PATH approach)
- **Verification:** `cmake --build /workspace/build --target DeepCShuffle` exits 0; `moc_ShuffleMatrixWidget.cpp` generated in autogen directory
- **Committed in:** `bcfe276` (Task 2 commit)

**2. [Rule 1 - Bug] `DD::Image::WidgetPointer` is not valid — global typedef**
- **Found during:** Task 1 (compile-test of ShuffleMatrixKnob.h)
- **Issue:** Header initially used `DD::Image::WidgetPointer` as the `make_widget()` return type; this caused compile error because `WidgetPointer` is a global typedef, not in the `DD::Image` namespace
- **Fix:** Changed return type to bare `WidgetPointer` (global scope)
- **Files modified:** src/ShuffleMatrixKnob.h
- **Verification:** `g++ -std=c++17 -I/usr/local/Nuke16.0v6/include -c ShuffleMatrixKnob.h` compiles clean
- **Committed in:** `4ef58a7` (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking prereq verification, 1 namespace bug)
**Impact on plan:** Both auto-fixes necessary for correctness. No scope creep.

## Issues Encountered

- `sudo apt-get update` not available (no sudo access); confirmed Qt6 6.5.3 at `/opt/Qt/6.5.3/gcc_64` is a complete dev install (headers + moc) — no apt install needed

## Next Phase Readiness

- Plan 03-03 can now implement `ShuffleMatrixKnob.cpp` — both header contracts are locked and include-safe
- Plan 03-04 can implement `ShuffleMatrixWidget.cpp` — Q_OBJECT and moc integration verified working
- Build baseline is clean with AUTOMOC enabled

---
*Phase: 03-deepshuffle-ui*
*Completed: 2026-03-15*
