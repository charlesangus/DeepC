---
phase: 03-deepshuffle-ui
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-03-14T19:52:52.805Z
---

<current_state>
Mid-way through /gsd:discuss-phase 3 (DeepShuffle UI). No CONTEXT.md written yet. The discussion
was interrupted during the 8-channel layout area — the user rejected a question form and was about
to describe how the UI should look. They then shared a reference screenshot of the legacy Nuke
Shuffle node (reference/shuffle.png) and the URL https://erwanleroy.com/nuke-for-beginners-the-shuffle-node/

The user said: "It should look like the old Nuke Shuffle node."

Two follow-up questions were queued (and rejected/interrupted before being answered):
1. Should DeepCShuffle have two Op inputs like the legacy Shuffle (in1 + in2)?
2. Is the dropout/dropdown-based per-channel UI acceptable (since the visual matrix grid is a
   custom built-in Nuke widget, not reproducible with standard NDK knobs)?
</current_state>

<completed_work>
- Phase 3 init ran: phase_found, no CONTEXT.md, no plans, phase_dir was null (created manually)
- Prior context loaded: PROJECT.md, REQUIREMENTS.md, STATE.md, Phase 1 + Phase 2 CONTEXT.md
- Codebase scouted: read full DeepCShuffle.cpp (currently 4 channel pairs, Input_Channel_knob +
  Channel_knob rows, no port labeling, no layer routing)
- Gray areas identified and presented: user selected all 4 (8-channel layout, layer routing
  SHUF-04, port labeling SHUF-02, passthrough behavior)
- Discussion started on "8-channel layout" area — user shared reference image before answering
</completed_work>

<remaining_work>
- Resume discuss-phase 3 from where it left off:
  - Finish "8-channel layout" discussion (user wants legacy Shuffle look — two inputs, matrix style)
  - Discuss layer routing (SHUF-04): Input_ChannelSet_knob placement and interaction
  - Discuss port labeling (SHUF-02): noodle ports vs knob labels vs both
  - Discuss passthrough behavior: unmapped channels pass through or zero?
- Write 03-CONTEXT.md once all areas discussed
- Git commit context
- Update STATE.md
</remaining_work>

<decisions_made>
- User wants DeepCShuffle to look like the legacy Nuke Shuffle node (reference/shuffle.png)
- Legacy Shuffle has: two Op inputs (in1, in2), ChannelSet pickers per input, visual matrix grid
  for per-channel routing, output labels on right side
- The visual matrix grid is NOT reproducible with NDK knobs — per-channel dropdowns are the
  standard NDK equivalent; user had not yet confirmed this trade-off is acceptable
- All 4 gray areas selected for discussion
</decisions_made>

<blockers>
- None hard. But two key questions are unanswered:
  1. Two Op inputs or one? (Legacy Shuffle has two)
  2. Is dropdown-based routing acceptable vs the visual matrix grid?
  These must be answered before CONTEXT.md can be written for the layout/interaction decisions.
</blockers>

<context>
Phase 3 requirements (SHUF-01 through SHUF-04):
- SHUF-01: per-channel routing knobs matching legacy Nuke Shuffle layout
- SHUF-02: input/output ports labeled with assigned channel names
- SHUF-03: support up to 8 channels (currently 4)
- SHUF-04: layer-level routing via Input_ChannelSet_knob

Current DeepCShuffle.cpp: 4 named members (_inChannel0.._inChannel3, _outChannel0.._outChannel3),
Input_Channel_knob + ">>" Text_knob + Channel_knob per row. Single Op input. No knob_changed.
codebase has Input_ChannelSet_knob in DeepCConstant, DeepCWrapper, DeepCMWrapper, DeepCID.
</context>

<next_action>
Resume /gsd:discuss-phase 3. Start by acknowledging the reference image and asking:
1. Two inputs (in1 + in2) like legacy Shuffle, or single input?
2. Dropdown-based per-channel routing OK (matrix grid not possible with NDK)?
Then continue through remaining gray areas (layer routing, port labeling, passthrough).
</next_action>
