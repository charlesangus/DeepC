---
phase: 04-deepcpnoise-4d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FastNoise/FastNoise.h
  - FastNoise/FastNoise.cpp
autonomous: true
requirements:
  - NOIS-01

must_haves:
  truths:
    - "SingleValue(offset, x, y, z, w) compiles and returns a valid interpolated noise value in 4D"
    - "SingleValueFractalFBM/Billow/RigidMulti(x,y,z,w) compile and return valid fractal values"
    - "SinglePerlin(offset, x, y, z, w) compiles and returns a valid gradient noise value in 4D"
    - "SinglePerlinFractalFBM/Billow/RigidMulti(x,y,z,w) compile and return valid fractal values"
    - "All new 4D method declarations are present in FastNoise.h"
  artifacts:
    - path: "FastNoise/FastNoise.h"
      provides: "4D method declarations for Value, Perlin, Cubic, Cellular, ValCoord4DFast"
      contains: "SingleValue.*w.*const"
    - path: "FastNoise/FastNoise.cpp"
      provides: "SingleValue(4D), SingleValueFractalFBM/Billow/RigidMulti(4D), SinglePerlin(4D), SinglePerlinFractalFBM/Billow/RigidMulti(4D)"
      contains: "SingleValue.*unsigned char offset.*w.*const"
  key_links:
    - from: "FastNoise/FastNoise.h"
      to: "FastNoise/FastNoise.cpp"
      via: "method declarations matched by implementations"
      pattern: "SingleValue.*FN_DECIMAL.*w"
---

<objective>
Add all 4D method declarations to FastNoise.h and implement 4D Value and 4D Perlin noise (base + 3 fractal variants each = 8 new functions) in FastNoise.cpp.

Purpose: Value and Perlin 4D require no new data tables — they use existing infrastructure (`Index4D_256`, `ValCoord4DFast`, `GradCoord4D`). Implementing them first confirms the 4D extension pattern compiles cleanly before tackling the more complex Cubic/Cellular algorithms in Plan 02.

Output: FastNoise.h fully declared for all new 4D types. FastNoise.cpp with Value and Perlin 4D implementations.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-deepcpnoise-4d/04-CONTEXT.md
@.planning/phases/04-deepcpnoise-4d/04-RESEARCH.md

<interfaces>
<!-- Existing FastNoise infrastructure reused by 4D Value and Perlin implementations -->

From FastNoise/FastNoise.h (private 3D section — pattern to match for 4D):
```cpp
// Existing 3D declarations (lines 273-281) — 4D variants follow identical structure with added `w`:
FN_DECIMAL SingleValueFractalFBM(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z) const;
FN_DECIMAL SingleValueFractalBillow(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z) const;
FN_DECIMAL SingleValueFractalRigidMulti(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z) const;
FN_DECIMAL SingleValue(unsigned char offset, FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z) const;

FN_DECIMAL SinglePerlinFractalFBM(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z) const;
FN_DECIMAL SinglePerlinFractalBillow(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z) const;
FN_DECIMAL SinglePerlinFractalRigidMulti(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z) const;
FN_DECIMAL SinglePerlin(unsigned char offset, FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z) const;

// Existing 4D declarations already in FastNoise.h (lines 298-302, 306-315):
FN_DECIMAL SingleSimplexFractalFBM(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleSimplexFractalBillow(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleSimplexFractalRigidMulti(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleSimplex(unsigned char offset, FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
inline unsigned char Index4D_32(unsigned char offset, int x, int y, int z, int w) const;
inline unsigned char Index4D_256(unsigned char offset, int x, int y, int z, int w) const;
inline FN_DECIMAL GradCoord4D(unsigned char offset, int x, int y, int z, int w, FN_DECIMAL xd, FN_DECIMAL yd, FN_DECIMAL zd, FN_DECIMAL wd) const;
// ValCoord4D (static, line 292): static FN_DECIMAL ValCoord4D(int seed, int x, int y, int z, int w)
// ValCoord3DFast (line 307): uses m_perm lookup — ValCoord4DFast follows same pattern with Index4D_256
```

Existing `SingleValue` 3D template (FastNoise.cpp lines 631-669):
```cpp
// Pattern: floor all axes → compute interp weights (xs, ys, zs) → Lerp 8 corners
// 4D Value: add w axis → compute ws weight → Lerp 16 corners (4D cascade)
// Uses ValCoord3DFast(offset, x, y, z) → 4D uses ValCoord4DFast(offset, x, y, z, w) [new helper]
```

Existing `SinglePerlin` 3D template (FastNoise.cpp lines 859-904):
```cpp
// Pattern: floor all axes → compute interp weights → GradCoord3D for 8 corners
// 4D Perlin: add w axis → GradCoord4D for 16 corners
// GradCoord4D already exists (uses Index4D_32 + GRAD_4D[32*4])
```

Existing `SingleSimplexFractalFBM` 4D (FastNoise.cpp lines 1059-1077) — NOTE ON FREQUENCY:
```cpp
// The 4D Simplex FBM applies m_frequency internally on line 1061.
// GetNoise(x,y,z,w) at line 333-335 scales x*m_frequency, y*m_frequency, z*m_frequency but NOT w.
// For new types: GetNoise(x,y,z,w) will scale ALL FOUR axes (x,y,z,w *= m_frequency) before calling
// the fractal wrapper. New 4D fractal wrappers must NOT apply m_frequency internally — they receive
// pre-scaled coordinates (matching the 3D fractal wrapper convention).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all 4D declarations to FastNoise.h</name>
  <files>FastNoise/FastNoise.h</files>
  <action>
In FastNoise.h, find the existing `//4D` private section (after line 298 where the 4D Simplex declarations are). Add ALL new 4D declarations immediately after the existing 4D Simplex block:

```cpp
// 4D Value
FN_DECIMAL SingleValueFractalFBM(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleValueFractalBillow(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleValueFractalRigidMulti(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleValue(unsigned char offset, FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;

// 4D Perlin
FN_DECIMAL SinglePerlinFractalFBM(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SinglePerlinFractalBillow(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SinglePerlinFractalRigidMulti(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SinglePerlin(unsigned char offset, FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;

// 4D Cubic
FN_DECIMAL SingleCubicFractalFBM(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleCubicFractalBillow(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleCubicFractalRigidMulti(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleCubic(unsigned char offset, FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;

// 4D Cellular
FN_DECIMAL SingleCellular(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;
FN_DECIMAL SingleCellular2Edge(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const;

// 4D helper
inline FN_DECIMAL ValCoord4DFast(unsigned char offset, int x, int y, int z, int w) const;
```

No implementation here — declarations only. This unblocks Plans 02 and 03 from being able to implement and call these methods.
  </action>
  <verify>
    <automated>cd /workspace/build && make FastNoise 2>&1 | grep -E "error:|note:" | head -20</automated>
  </verify>
  <done>FastNoise.h contains all new 4D declarations. `make FastNoise` reports no errors from the header additions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 4D Value and 4D Perlin in FastNoise.cpp</name>
  <files>FastNoise/FastNoise.cpp</files>
  <action>
Add the following implementations to FastNoise.cpp. Place them immediately after the existing 3D Value and 3D Perlin implementations (after line 669 and after line 904 respectively), keeping type groupings consistent.

**ValCoord4DFast helper** (add near `ValCoord3DFast` at line 307):
```cpp
inline FN_DECIMAL FastNoise::ValCoord4DFast(unsigned char offset, int x, int y, int z, int w) const
{
    return VAL_LUT[Index4D_256(offset, x, y, z, w)];
}
```

**SingleValue 4D** (add after existing SingleValue 3D at line 669):
```cpp
FN_DECIMAL FastNoise::SingleValue(unsigned char offset, FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const
{
    int x0 = FastFloor(x);
    int y0 = FastFloor(y);
    int z0 = FastFloor(z);
    int w0 = FastFloor(w);
    int x1 = x0 + 1;
    int y1 = y0 + 1;
    int z1 = z0 + 1;
    int w1 = w0 + 1;

    FN_DECIMAL xs, ys, zs, ws;
    switch (m_interp)
    {
    case Linear:
        xs = x - (FN_DECIMAL)x0;
        ys = y - (FN_DECIMAL)y0;
        zs = z - (FN_DECIMAL)z0;
        ws = w - (FN_DECIMAL)w0;
        break;
    case Hermite:
        xs = InterpHermiteFunc(x - (FN_DECIMAL)x0);
        ys = InterpHermiteFunc(y - (FN_DECIMAL)y0);
        zs = InterpHermiteFunc(z - (FN_DECIMAL)z0);
        ws = InterpHermiteFunc(w - (FN_DECIMAL)w0);
        break;
    case Quintic:
        xs = InterpQuinticFunc(x - (FN_DECIMAL)x0);
        ys = InterpQuinticFunc(y - (FN_DECIMAL)y0);
        zs = InterpQuinticFunc(z - (FN_DECIMAL)z0);
        ws = InterpQuinticFunc(w - (FN_DECIMAL)w0);
        break;
    }

    // 16-corner quadrilinear interpolation (2^4 corners, lerped dimension by dimension)
    FN_DECIMAL xf000 = Lerp(ValCoord4DFast(offset, x0, y0, z0, w0), ValCoord4DFast(offset, x1, y0, z0, w0), xs);
    FN_DECIMAL xf100 = Lerp(ValCoord4DFast(offset, x0, y1, z0, w0), ValCoord4DFast(offset, x1, y1, z0, w0), xs);
    FN_DECIMAL xf010 = Lerp(ValCoord4DFast(offset, x0, y0, z1, w0), ValCoord4DFast(offset, x1, y0, z1, w0), xs);
    FN_DECIMAL xf110 = Lerp(ValCoord4DFast(offset, x0, y1, z1, w0), ValCoord4DFast(offset, x1, y1, z1, w0), xs);

    FN_DECIMAL xf001 = Lerp(ValCoord4DFast(offset, x0, y0, z0, w1), ValCoord4DFast(offset, x1, y0, z0, w1), xs);
    FN_DECIMAL xf101 = Lerp(ValCoord4DFast(offset, x0, y1, z0, w1), ValCoord4DFast(offset, x1, y1, z0, w1), xs);
    FN_DECIMAL xf011 = Lerp(ValCoord4DFast(offset, x0, y0, z1, w1), ValCoord4DFast(offset, x1, y0, z1, w1), xs);
    FN_DECIMAL xf111 = Lerp(ValCoord4DFast(offset, x0, y1, z1, w1), ValCoord4DFast(offset, x1, y1, z1, w1), xs);

    FN_DECIMAL yf00 = Lerp(xf000, xf100, ys);
    FN_DECIMAL yf10 = Lerp(xf010, xf110, ys);
    FN_DECIMAL yf01 = Lerp(xf001, xf101, ys);
    FN_DECIMAL yf11 = Lerp(xf011, xf111, ys);

    FN_DECIMAL zf0 = Lerp(yf00, yf10, zs);
    FN_DECIMAL zf1 = Lerp(yf01, yf11, zs);

    return Lerp(zf0, zf1, ws);
}
```

**SingleValueFractalFBM 4D** (follows SingleSimplexFractalFBM 4D pattern, but pre-scaled — receives already-scaled coords):
```cpp
FN_DECIMAL FastNoise::SingleValueFractalFBM(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const
{
    FN_DECIMAL sum = SingleValue(m_perm[0], x, y, z, w);
    FN_DECIMAL amp = 1;
    int i = 0;

    while (++i < m_octaves)
    {
        x *= m_lacunarity;
        y *= m_lacunarity;
        z *= m_lacunarity;
        w *= m_lacunarity;

        amp *= m_gain;
        sum += SingleValue(m_perm[i], x, y, z, w) * amp;
    }

    return sum * m_fractalBounding;
}
```

**SingleValueFractalBillow 4D:**
```cpp
FN_DECIMAL FastNoise::SingleValueFractalBillow(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const
{
    FN_DECIMAL sum = FastAbs(SingleValue(m_perm[0], x, y, z, w)) * 2 - 1;
    FN_DECIMAL amp = 1;
    int i = 0;

    while (++i < m_octaves)
    {
        x *= m_lacunarity;
        y *= m_lacunarity;
        z *= m_lacunarity;
        w *= m_lacunarity;

        amp *= m_gain;
        sum += (FastAbs(SingleValue(m_perm[i], x, y, z, w)) * 2 - 1) * amp;
    }

    return sum * m_fractalBounding;
}
```

**SingleValueFractalRigidMulti 4D:**
```cpp
FN_DECIMAL FastNoise::SingleValueFractalRigidMulti(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const
{
    FN_DECIMAL sum = 1 - FastAbs(SingleValue(m_perm[0], x, y, z, w));
    FN_DECIMAL amp = 1;
    int i = 0;

    while (++i < m_octaves)
    {
        x *= m_lacunarity;
        y *= m_lacunarity;
        z *= m_lacunarity;
        w *= m_lacunarity;

        amp *= m_gain;
        sum -= (1 - FastAbs(SingleValue(m_perm[i], x, y, z, w))) * amp;
    }

    return sum;
}
```

**SinglePerlin 4D** (add after existing SinglePerlin 3D at line 904):
```cpp
FN_DECIMAL FastNoise::SinglePerlin(unsigned char offset, FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const
{
    int x0 = FastFloor(x);
    int y0 = FastFloor(y);
    int z0 = FastFloor(z);
    int w0 = FastFloor(w);
    int x1 = x0 + 1;
    int y1 = y0 + 1;
    int z1 = z0 + 1;
    int w1 = w0 + 1;

    FN_DECIMAL xs, ys, zs, ws;
    switch (m_interp)
    {
    case Linear:
        xs = x - (FN_DECIMAL)x0;
        ys = y - (FN_DECIMAL)y0;
        zs = z - (FN_DECIMAL)z0;
        ws = w - (FN_DECIMAL)w0;
        break;
    case Hermite:
        xs = InterpHermiteFunc(x - (FN_DECIMAL)x0);
        ys = InterpHermiteFunc(y - (FN_DECIMAL)y0);
        zs = InterpHermiteFunc(z - (FN_DECIMAL)z0);
        ws = InterpHermiteFunc(w - (FN_DECIMAL)w0);
        break;
    case Quintic:
        xs = InterpQuinticFunc(x - (FN_DECIMAL)x0);
        ys = InterpQuinticFunc(y - (FN_DECIMAL)y0);
        zs = InterpQuinticFunc(z - (FN_DECIMAL)z0);
        ws = InterpQuinticFunc(w - (FN_DECIMAL)w0);
        break;
    }

    FN_DECIMAL xd0 = x - (FN_DECIMAL)x0;
    FN_DECIMAL yd0 = y - (FN_DECIMAL)y0;
    FN_DECIMAL zd0 = z - (FN_DECIMAL)z0;
    FN_DECIMAL wd0 = w - (FN_DECIMAL)w0;
    FN_DECIMAL xd1 = xd0 - 1;
    FN_DECIMAL yd1 = yd0 - 1;
    FN_DECIMAL zd1 = zd0 - 1;
    FN_DECIMAL wd1 = wd0 - 1;

    // 16-corner quadrilinear interpolation using GradCoord4D
    FN_DECIMAL xf000 = Lerp(GradCoord4D(offset, x0, y0, z0, w0, xd0, yd0, zd0, wd0), GradCoord4D(offset, x1, y0, z0, w0, xd1, yd0, zd0, wd0), xs);
    FN_DECIMAL xf100 = Lerp(GradCoord4D(offset, x0, y1, z0, w0, xd0, yd1, zd0, wd0), GradCoord4D(offset, x1, y1, z0, w0, xd1, yd1, zd0, wd0), xs);
    FN_DECIMAL xf010 = Lerp(GradCoord4D(offset, x0, y0, z1, w0, xd0, yd0, zd1, wd0), GradCoord4D(offset, x1, y0, z1, w0, xd1, yd0, zd1, wd0), xs);
    FN_DECIMAL xf110 = Lerp(GradCoord4D(offset, x0, y1, z1, w0, xd0, yd1, zd1, wd0), GradCoord4D(offset, x1, y1, z1, w0, xd1, yd1, zd1, wd0), xs);

    FN_DECIMAL xf001 = Lerp(GradCoord4D(offset, x0, y0, z0, w1, xd0, yd0, zd0, wd1), GradCoord4D(offset, x1, y0, z0, w1, xd1, yd0, zd0, wd1), xs);
    FN_DECIMAL xf101 = Lerp(GradCoord4D(offset, x0, y1, z0, w1, xd0, yd1, zd0, wd1), GradCoord4D(offset, x1, y1, z0, w1, xd1, yd1, zd0, wd1), xs);
    FN_DECIMAL xf011 = Lerp(GradCoord4D(offset, x0, y0, z1, w1, xd0, yd0, zd1, wd1), GradCoord4D(offset, x1, y0, z1, w1, xd1, yd0, zd1, wd1), xs);
    FN_DECIMAL xf111 = Lerp(GradCoord4D(offset, x0, y1, z1, w1, xd0, yd1, zd1, wd1), GradCoord4D(offset, x1, y1, z1, w1, xd1, yd1, zd1, wd1), xs);

    FN_DECIMAL yf00 = Lerp(xf000, xf100, ys);
    FN_DECIMAL yf10 = Lerp(xf010, xf110, ys);
    FN_DECIMAL yf01 = Lerp(xf001, xf101, ys);
    FN_DECIMAL yf11 = Lerp(xf011, xf111, ys);

    FN_DECIMAL zf0 = Lerp(yf00, yf10, zs);
    FN_DECIMAL zf1 = Lerp(yf01, yf11, zs);

    return Lerp(zf0, zf1, ws);
}
```

**SinglePerlinFractalFBM 4D** (receives pre-scaled coords from GetNoise — does NOT apply m_frequency internally):
```cpp
FN_DECIMAL FastNoise::SinglePerlinFractalFBM(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const
{
    FN_DECIMAL sum = SinglePerlin(m_perm[0], x, y, z, w);
    FN_DECIMAL amp = 1;
    int i = 0;

    while (++i < m_octaves)
    {
        x *= m_lacunarity;
        y *= m_lacunarity;
        z *= m_lacunarity;
        w *= m_lacunarity;

        amp *= m_gain;
        sum += SinglePerlin(m_perm[i], x, y, z, w) * amp;
    }

    return sum * m_fractalBounding;
}
```

**SinglePerlinFractalBillow 4D:**
```cpp
FN_DECIMAL FastNoise::SinglePerlinFractalBillow(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const
{
    FN_DECIMAL sum = FastAbs(SinglePerlin(m_perm[0], x, y, z, w)) * 2 - 1;
    FN_DECIMAL amp = 1;
    int i = 0;

    while (++i < m_octaves)
    {
        x *= m_lacunarity;
        y *= m_lacunarity;
        z *= m_lacunarity;
        w *= m_lacunarity;

        amp *= m_gain;
        sum += (FastAbs(SinglePerlin(m_perm[i], x, y, z, w)) * 2 - 1) * amp;
    }

    return sum * m_fractalBounding;
}
```

**SinglePerlinFractalRigidMulti 4D:**
```cpp
FN_DECIMAL FastNoise::SinglePerlinFractalRigidMulti(FN_DECIMAL x, FN_DECIMAL y, FN_DECIMAL z, FN_DECIMAL w) const
{
    FN_DECIMAL sum = 1 - FastAbs(SinglePerlin(m_perm[0], x, y, z, w));
    FN_DECIMAL amp = 1;
    int i = 0;

    while (++i < m_octaves)
    {
        x *= m_lacunarity;
        y *= m_lacunarity;
        z *= m_lacunarity;
        w *= m_lacunarity;

        amp *= m_gain;
        sum -= (1 - FastAbs(SinglePerlin(m_perm[i], x, y, z, w))) * amp;
    }

    return sum;
}
```

NOTE: Do NOT implement the Cubic and Cellular 4D functions in this task — those go in Plan 02. The header already has their declarations; the .cpp stubs are NOT needed here (the linker will complain only when a caller is added, which happens in Plan 03). If the compiler produces "undefined reference" warnings at this stage, that is expected and acceptable — they will be resolved in Plans 02 and 03.
  </action>
  <verify>
    <automated>cd /workspace/build && make FastNoise 2>&1 | grep -E "^.*error:" | head -20</automated>
  </verify>
  <done>FastNoise.cpp compiles cleanly for the Value and Perlin 4D implementations. No errors from these new functions. (Undefined reference warnings for unimplemented Cubic/Cellular declarations are acceptable at this stage.)</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -c "SingleValue.*unsigned char offset.*FN_DECIMAL x.*FN_DECIMAL y.*FN_DECIMAL z.*FN_DECIMAL w" /workspace/FastNoise/FastNoise.cpp` returns 1
- `grep -c "SinglePerlin.*unsigned char offset.*FN_DECIMAL x.*FN_DECIMAL y.*FN_DECIMAL z.*FN_DECIMAL w" /workspace/FastNoise/FastNoise.cpp` returns 1
- `grep -c "SingleValueFractal" /workspace/FastNoise/FastNoise.cpp | awk '{if($1>=6) print "OK"}'` (3 existing 3D + 3 new 4D = 6+)
- `cd /workspace/build && make FastNoise 2>&1 | grep -c "^.*error:"` returns 0
</verification>

<success_criteria>
- FastNoise.h contains 4D declarations for Value (4 methods), Perlin (4 methods), Cubic (4 methods), Cellular (2 methods), and ValCoord4DFast
- FastNoise.cpp implements ValCoord4DFast, SingleValue(4D), SingleValueFractal{FBM,Billow,RigidMulti}(4D), SinglePerlin(4D), SinglePerlinFractal{FBM,Billow,RigidMulti}(4D)
- All new fractal wrappers scale w with `w *= m_lacunarity` inside the octave loop
- No new functions apply `m_frequency` internally (pre-scaled convention)
- `make FastNoise` compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-deepcpnoise-4d/04-01-SUMMARY.md`
</output>
