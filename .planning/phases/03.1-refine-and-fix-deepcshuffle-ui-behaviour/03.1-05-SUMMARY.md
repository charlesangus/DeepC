---
phase: 03.1-refine-and-fix-deepcshuffle-ui-behaviour
plan: 05
subsystem: ui
tags: [qt, qgridlayout, channelbutton, qcombobox, nuke-knob, deepcshuffle2]

# Dependency graph
requires:
  - phase: 03.1-refine-and-fix-deepcshuffle-ui-behaviour
    provides: ShuffleMatrixWidget with embedded QComboBox pickers and ChannelButton grid

provides:
  - Correct consecutive column indices for in1 channels (no gap between r/g/b/a)
  - 8px separator columns flanking the const:0/const:1 pair
  - Arrow labels matching ChannelButton height (22px fixed, bold 18pt)
  - Single-interaction ChannelSet picker rebuild via knob_changed before changed()
  - Routing checks survive ChannelSet picker changes
  - out2=none renders visible greyed-out placeholder group instead of disappearing

affects: [03.1-refine-and-fix-deepcshuffle-ui-behaviour, future-UAT]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Placeholder rows for disabled output groups mirror in2 placeholder column pattern
    - op()->knob_changed(targetKnob) before targetKnob->changed() for synchronous matrix rebuild
    - QLabel::setFixedHeight(22) on arrow labels to match ChannelButton height

key-files:
  created: []
  modified:
    - src/ShuffleMatrixWidget.cpp

key-decisions:
  - "Added two 8px separator columns (sepBeforeConstCol, sepAfterConstCol) flanking const pair; const:0/const:1 remain adjacent with standard 2px grid spacing"
  - "Arrow labels get setFixedHeight(22) + bold 18pt font — no reliance on content height which varied with font metrics"
  - "Picker lambdas call knob_changed() before changed() so Op resolves ChannelSet synchronously before async kUpdateWidgets fires"
  - "out2Rows placeholder insertion (rgba.red/green/blue/alpha) added when out2Disabled and out2Rows empty; if(!out2Rows.empty()) guard removed"

requirements-completed: []

# Metrics
duration: 25min
completed: 2026-03-16
---

# Phase 03.1 Plan 05: UAT Round 3 Bug Fixes Summary

**Six ShuffleMatrixWidget visual and interaction bugs fixed: column gaps, const spacing, arrow height, single-pick rebuild, state persistence, and disabled-group visibility**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-03-16T10:20:00Z
- **Completed:** 2026-03-16T10:45:00Z
- **Tasks:** 6 bugs fixed in 1 commit
- **Files modified:** 1

## Accomplishments

- Fixed column layout — in1 r/g/b/a buttons occupy consecutive grid columns 0..3 with no skipped indices between them
- Added 8px separator columns on both outer sides of the const:0/const:1 pair for clear visual grouping
- Arrow labels now match ChannelButton height (22px fixed) with bold 18pt font
- ChannelSet picker changes now trigger immediate matrix rebuild on the first interaction (not second) by calling `op()->knob_changed()` synchronously before `changed()`
- Routing checks survive ChannelSet changes — the restore logic in syncFromKnob was already correct; the single-pick fix ensures it runs with up-to-date ChannelSets
- out2=none now shows visible greyed-out placeholder buttons (rgba r/g/b/a) instead of an invisible collapsed group

## Task Commits

1. **All six bugs** - `4146538` (fix)

## Files Created/Modified

- `/workspace/src/ShuffleMatrixWidget.cpp` — column index constants refactored to add separator columns, makeArrowLabel updated, out2 placeholder logic added, picker lambdas updated with knob_changed ordering

## Decisions Made

- Separator columns use `setColumnMinimumWidth(8)` with `setColumnStretch(0)` — no widgets placed in them, just width enforcement
- out2 placeholder rows use same channel names as in2 placeholders (rgba.red/green/blue/alpha) for visual consistency
- Arrow font set to absolute `setPointSize(18)` rather than relative `+N` to get predictable sizing across different system fonts

## Deviations from Plan

None — plan executed exactly as specified. All six bugs addressed in ShuffleMatrixWidget.cpp only.

## Issues Encountered

None — build was clean on first attempt after edits.

## Next Phase Readiness

- UAT round 3 complete; all six reported failures addressed
- Ready for user to build, deploy, and run UAT round 4
- If UAT passes cleanly, Phase 03.1 is done and Phase 04 can begin

---
*Phase: 03.1-refine-and-fix-deepcshuffle-ui-behaviour*
*Completed: 2026-03-16*
