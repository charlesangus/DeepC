---
phase: 03.1-refine-and-fix-deepcshuffle-ui-behaviour
task: UAT round 4 implemented — final UAT pending
total_tasks: UAT round 4 complete
status: in_progress
last_updated: 2026-03-17T01:09:37.492Z
---

<current_state>
All 5 UAT round 4 bugs have been implemented across 3 commits. Phase 03.1 is code-complete
pending final UAT in Nuke. There are uncommitted changes in the working tree.

**Commits since last pause:**
- `01bf39c` — Bug1+4+5: ArrowLabel custom QPainter widget, in2Picker span fix, disabled color
- `ba061ad` — Bug3 diagnostic: DeepCTest node added to confirm ChannelSet synchrony
- `ba17c6f` — Bug2: positional state format (out1:0:in1:2,...) + DeepCTest removed

**Uncommitted changes (working tree):**
- `.planning/REQUIREMENTS.md` — staged: SHUF-01..04 marked [x] complete, coverage table updated
- `src/DeepCPMatte.cpp` — unstaged: live knob value reads in draw_handle() (Phase 4 prep)
- `src/DeepCTest.cpp` — deleted in working tree but not staged (was removed in spirit by ba17c6f)
</current_state>

<completed_work>

- Plan 03.1-01: Logic bugs (radio no-op, outGroup scope, const:0/1 columns, identity init) ✓
- Plan 03.1-02: Disabled-not-hidden and single-step ChannelSet lag ✓
- Plan 03.1-03: ChannelButton colored X-mark, nukeChannelColor, arrow decorators ✓
- Plan 03.1-04: QComboBox pickers with QTimer deferral, NDK knobs INVISIBLE, layout ✓
- UAT round 1 fixes (7 commits) ✓
- UAT round 2 fixes (6 commits) ✓
- UAT round 3 fixes (6 fixes committed) ✓
- UAT round 4 — all 5 bugs implemented ✓:
  - Bug1: ArrowLabel custom QPainter widget (setFixedSize 22×22, Down/Right direction) ✓
  - Bug2: Positional state format "out1:0:in1:2,..." + migration in from_script() + _validate() positional parsing ✓
  - Bug3: DeepCTest diagnostic confirmed ChannelSet synchrony → fix applied → DeepCTest removed ✓
  - Bug4: in2Picker starts at in2StartCol (not sepBeforeConstCol) — separator columns preserved ✓
  - Bug5: Disabled fill QColor(85,85,85) + border QColor(110,110,110) ✓
</completed_work>

<remaining_work>

1. **Commit the uncommitted changes cleanly:**
   - Stage `src/DeepCTest.cpp` deletion (should have been in ba17c6f, needs its own commit)
   - Commit `.planning/REQUIREMENTS.md` (SHUF requirements marked complete) — can bundle with deletion
   - `src/DeepCPMatte.cpp` is Phase 4 prep work — stash or commit separately, do NOT mix into 03.1 closeout

2. **Run final UAT round in Nuke on the round 4 fixes:**
   - ArrowLabel rendering (Bug1) — visual check against reference/shuffle.png
   - Positional state round-trip (Bug2) — change ChannelSet, verify routing persists; save/reload .nk script
   - ChannelSet single-step update (Bug3) — confirm one-pick updates matrix headers immediately
   - in2 separator columns visible (Bug4) — confirm two 8px gaps still present around const:0/1
   - Disabled button contrast (Bug5) — verify buttons visible on Nuke's ~#2b2b2b dark background

3. **Close out phase 03.1:**
   - Write UAT round 4 results (new pass section in VERIFICATION.md or separate document)
   - Update STATE.md (mark 03.1 complete, advance to Phase 4)
   - Delete stale .continue-here.md files (this one + 03-deepshuffle-ui/.continue-here.md)
   - Ask user about pushing to remote
</remaining_work>

<decisions_made>

- ArrowLabel: QPainter custom widget (same pattern as ChannelButton), not QLabel font tricks
- State format: positional indexing "outputGroup:outRowIdx:sourceGroup:sourceColIdx"
- Bug3 fix: DeepCTest diagnostic confirmed set_text() IS synchronous; fix was in the call order in knob_changed; DeepCTest then removed
- DeepCPMatte.cpp changes are Phase 4 prep (live knob reads in draw_handle); they should be
  committed separately from 03.1 phase-close work
- QComboBox pickers remain embedded (not reverting to NDK knobs)
- NDK ChannelSet knobs remain INVISIBLE
</decisions_made>

<blockers>

- None. All bugs fixed. Final UAT requires Nuke access.
</blockers>

<context>
Phase 03.1 has gone through 4 UAT rounds. Each round found bugs invisible to automated checking.
The positional state format (Bug2) was the fundamental fix — channel-name-based state caused
routing to vanish when the user changed a ChannelSet. Positional indexing survives ChannelSet
changes correctly.

The DeepCPMatte.cpp changes in the working tree are Phase 4 work (GL handle live-knob reads)
that appear to have been started already. They should not be mixed into 03.1 closeout commits.

The existing VERIFICATION.md for 03.1 was generated after plans 01-04 (before UAT round 4 fixes).
It covers the code-level verification; a UAT round 4 pass section should be appended after
final Nuke testing.
</context>

<next_action>
1. Sort out uncommitted changes:
   - `git add src/DeepCTest.cpp` (stage deletion) + `git commit` bundled with REQUIREMENTS.md as 03.1 docs closeout
   - Stash or separate-commit DeepCPMatte.cpp (Phase 4 work, not 03.1)
2. Run final UAT in Nuke for round 4 fixes (5 items above)
3. Append UAT round 4 results to VERIFICATION.md, update STATE.md, clean up .continue-here files
</next_action>
