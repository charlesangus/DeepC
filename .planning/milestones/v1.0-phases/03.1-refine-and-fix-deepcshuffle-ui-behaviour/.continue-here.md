---
phase: 03.1-refine-and-fix-deepcshuffle-ui-behaviour
task: done
total_tasks: 4
status: awaiting_human_uat
last_updated: 2026-03-17T04:22:08.023Z
---

<current_state>
All 4 plans are implemented and committed. UAT rounds 1–5 are complete with all bugs fixed. Build is clean (11/11 automated verifications pass). The phase is blocked on human UAT in Nuke — 10 runtime/visual items cannot be verified by code inspection alone. Verification and Validation docs are untracked (not yet committed).
</current_state>

<completed_work>

- Plan 01: Radio no-op guard, scoped radio enforcement, const:0/1 columns, identity routing init, four-field objectName format — Done
- Plan 02: disabled-not-hidden for none ChannelSet, single-step ChannelSet update (make_widget calls syncFromKnob after construction) — Done
- Plan 03: ChannelButton with paintEvent + colored X-mark, nukeChannelColor(), down/right arrow decorators — Done
- Plan 04: QComboBox embedded pickers attempted → segfault → reverted; switched to native NDK ChannelSet knobs ordered above/below matrix; spacer column removed; INVISIBLE flag removed — Done
- UAT rounds 1–5: All bugs fixed (ArrowLabel right-arrow, stale ChannelSet on panel open, out2 picker disabled state, picker maxWidth cap)
- VERIFICATION.md: All 11 automated truths verified — status human_needed
</completed_work>

<remaining_work>

- Commit untracked docs: 03-VERIFICATION.md, 03.1-VALIDATION.md, 03.1-VERIFICATION.md, .gitkeep, reference/ directory
- Human UAT in Nuke (10 items — all require running Nuke with the built plugin):
  1. Radio no-op: click already-checked button, stays checked
  2. Radio scope isolation: out1/out2 groups don't interfere
  3. const:0/const:1 render values (black/white output)
  4. Identity routing on fresh node first open
  5. Disabled-not-hidden for none ChannelSet (in2=Chan_Black)
  6. Single-step ChannelSet update (column headers rebuild immediately)
  7. Colored buttons with X-mark (R=red, G=green, B=blue, A=grey)
  8. Arrow decorators match reference/shuffle.png
  9. NDK ChannelSet knob layout (in1/in2 above matrix, out1/out2 below)
  10. Script save/load round-trip
- After UAT passes: phase complete → push to remote
</remaining_work>

<decisions_made>

- QComboBox embedded pickers abandoned due to segfault when calling set_text()+knob_changed() from within currentTextChanged signal handler inside Nuke's panel refresh cycle; replaced with native NDK ChannelSet knobs
- Knob ordering: in1/in2 registered before CustomKnob2 (matrix widget), out1/out2 registered after — gives panel layout: in knobs → matrix → out knobs
- No INVISIBLE flag on any ChannelSet knob (reverted from earlier approach)
- in2StartCol = in1Count + 2 (not + 3): const:0 at in1Count, const:1 at in1Count+1, then in2 — no spacer column
- Four-field button objectName: outGroup|outputName|sourceGroup|sourceName
</decisions_made>

<blockers>

- Nuke UAT blocked: requires Nuke with the built .so plugin loaded — cannot be done in this environment. User must run Nuke manually and test the 10 items listed in 03.1-VERIFICATION.md → "Human Verification Required" section.
</blockers>

<context>
Phase 03.1 is code-complete. All four plans have been implemented, all UAT bugs across 5 rounds have been fixed. The verification agent confirmed 11/11 automated checks pass. The only remaining gate is a human running the 10 runtime/visual tests in Nuke. Once UAT passes, commit the untracked docs, confirm UAT complete, and push to remote.

The VERIFICATION.md at .planning/phases/03.1-.../03.1-VERIFICATION.md has detailed step-by-step instructions for each UAT item. The reference image is at reference/shuffle.png.
</context>

<next_action>
Start with: commit untracked docs (03-VERIFICATION.md, 03.1-VALIDATION.md, 03.1-VERIFICATION.md, .gitkeep, reference/) then run Nuke UAT against the 10 items in 03.1-VERIFICATION.md → "Human Verification Required" section.
</next_action>
