# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M002 | arch | DeepCBlur base class | Direct DeepFilterOp subclass | DeepPixelOp prohibits spatial ops; DeepCWrapper can't expand input bbox; DeepFilterOp gives full control over getDeepRequests() and doDeepEngine() | No | agent |
| D002 | M002 | arch | Blur filter type | Gaussian only | User specified Gaussian only. Keeps implementation focused and knob layout simple. Non-Gaussian types out of scope. | Yes — if user requests box/triangle later | agent |
| D003 | M002 | arch | Size knob layout | Separate width and height float knobs | User specified separate w/h for non-uniform (anisotropic) blur. Matches Nuke built-in Blur pattern. | No | agent |
| D004 | M002 | arch | Channel selection | Blur all channels, no channel selector knob | Deep images carry all channels per-sample; blurring a subset creates inconsistency between blurred and unblurred channels within the same sample. User explicitly rejected channel selection. | Yes — if a valid per-sample-channel-subset use case emerges | agent |
| D005 | M002 | arch | Sample propagation | Propagate samples into empty neighboring pixels | Without propagation, blur is invisible in sparse regions. Propagation creates new samples in empty pixels with Gaussian-weighted color from source samples. Depth values propagate from source. | No | agent |
| D006 | M002 | arch | Sample optimization location | Per-pixel inside doDeepEngine(), not as a separate downstream node | Doing optimization per-pixel before writing to the output plane avoids materializing inflated sample counts in Nuke's tile cache. Avoids memory spike between DeepCBlur and downstream nodes. | Yes — if a post-blur optimization node proves more flexible | agent |
| D007 | M002 | pattern | Sample optimization modularity | Shared DeepSampleOptimizer.h header, reusable by future deep spatial ops | User emphasized modularity so future ops (DeepDefocus, etc.) can reuse the same optimization logic without code duplication. | No | agent |
| D008 | M003 | arch | Blur decomposition strategy | In-memory separable passes within single doDeepEngine, no OpTree | CMG99 used chained DeepOnlyOps via an OpTree framework (20 files). We keep our single-file approach with H+V passes sequentially in doDeepEngine using an intermediate per-pixel sample buffer. Simpler, no new framework. | No | agent |
| D009 | M003 | arch | Size knob layout | WH_knob with double[2] (supersedes D003) | User requested matching Nuke's built-in Blur convention. WH_knob provides a single line with w/h components and a lock toggle. Follows DeepCAdjustBBox pattern. | No | agent |
| D010 | M003 | arch | Alpha correction default | Off by default | Correction changes the visual output — off preserves backward compatibility with M002. Artists opt in explicitly. | Yes — if correction proves universally needed | agent |
| D011 | M003/S01 | arch | Sigma convention for kernel tiers | blur / 3.0 (not CMG99's 0.42466) | Maintains backward compatibility with M002 output. CMG99's factor would shift blur appearance for existing scripts. All three tiers (LQ/MQ/HQ) use this same sigma. | Yes — if parity with CMG99 reference output is required | agent |
| D012 | M003/S01 | arch | optimizeSamples call placement | After V pass only, never between H and V passes | Calling optimizeSamples after H pass would merge samples that still need to accumulate vertical weights, producing incorrect blur. Optimization must see the final fully-blurred sample set. | No | agent |
| D013 | M003/S01 | arch | Intermediate buffer indexing | Helper lambdas intX/intY for coordinate translation | Off-by-one errors in buffer indexing are silent (wrong blur, no crash). Lambdas make index arithmetic explicit and reviewable. Follow this pattern in future deep spatial ops using intermediate buffers. | No | agent |
| D014 | M003/S02 | arch | Alpha correction division guard | Skip correction when cumTransp < 1e-6 | Samples behind fully opaque layers have near-zero cumulative transmittance. Dividing by values below this threshold produces near-infinite or NaN channel values. Guard prevents visible speckle artifacts at hard depth boundaries. | Yes — if tighter threshold needed for specific render types | agent |
| D015 | M004-ks4br0/S01 | arch | colorDistance unpremult strategy | Divide channels by per-sample alpha; treat alpha < 1e-6 as always-matching | Channels in SampleRecord are premultiplied by alpha and Gaussian weight. Comparing premultiplied values confounds colour with coverage. Dividing by alpha recovers true colour. Near-zero alpha guard prevents division-by-zero for transparent samples — these should never prevent a merge. | No | agent |
| D016 | M004-ks4br0/S01 | arch | Overlap tidy pre-pass placement | Before existing tolerance-based merge pass in optimizeSamples | Overlapping intervals must be split and composited before colour-distance grouping can be correct — the compositing order changes channel values, which affects colour comparison. | No | agent |
| D017 | M004-ks4br0/S02 | arch | DeepCDepthBlur flatten invariant mechanism | Distribute source sample alpha via normalised falloff weights (sum=1); channels scale proportionally | This is the only approach that preserves the premultiplied relationship and guarantees flatten(output)==flatten(input) without post-hoc correction. | No | agent |
| D018 | M004-ks4br0/S02 | arch | DeepCDepthBlur "smooth" falloff mode | Smoothstep (3t²-2t³) | User confirmed. Zero derivative at both ends gives natural soft-edge feel for holdout softening. | Yes — if cosine or other S-curve preferred | agent |
| D019 | M004-ks4br0/S02 | arch | DeepCDepthBlur B-input gating criterion | Depth-range intersection: [zFront-spread, zBack+spread] ∩ [zFront_B, zBack_B] | User confirmed depth-overlap (not pixel presence). Only spreads where a DeepMerge would actually interact. | No | agent |
| D020 | M005-9j5er8 | convention | DeepCDepthBlur second input label | Main input unlabelled (""), second input labeled "ref" (supersedes "B" label from M004) | Nuke convention reserves "B" for the main/background input. "ref" is appropriate for an optional reference/gating input. Always visible pipe, not an extra input. | No | agent |
| D021 | M009-mso5fb | arch | DeepCOpenDefocus convolution engine | Fork opendefocus (EUPL-1.2), modify kernel for native deep sample support, build as static lib linked from CMake | opendefocus provides GPU-accelerated convolution with rich non-uniform bokeh artifacts (catseye, barndoors, astigmatism, axial aberration, inverse foreground). Forking allows deep sample kernel modifications while preserving the upstream license and CXX FFI pattern. Native deep support in the kernel avoids layer-slicing approximations and gives correct per-sample CoC and depth-ordered compositing. | No | collaborative |
| D022 | M009-mso5fb | arch | Rust↔C++ FFI approach for DeepCOpenDefocus | CXX crate (matching opendefocus-nuke pattern) | The existing opendefocus Nuke plugin uses CXX for its Rust↔C++ bridge. Following the same pattern reduces risk, stays close to upstream, and is the proven approach for this specific library. CXX provides type-safe interop without manual extern "C" or cbindgen. | No | collaborative |
| D023 | M009-mso5fb | arch | Build system integration for Rust dependency | Separate Rust build (cargo → static .a) linked from CMake; docker-build.sh extended with Rust stable + nightly toolchains | Keeps Rust and C++ build systems cleanly separated. Static linking avoids runtime Rust library dependencies. docker-build.sh is the established CI/release gate — extending it is the minimal-risk approach. Avoids corrosion/cmake-cargo complexity. | Yes — if corrosion CMake integration proves simpler | collaborative |
| D024 | M009-mso5fb | arch | Depth math mode for DeepCOpenDefocus CoC computation | Real depth math only (from DeepFront sample values) | Deep samples carry real world-unit depth values directly in DeepFront/DeepBack. No need for 1/z or Direct modes — the depth is already real. Simplifies the interface and avoids confusion. | Yes — if Direct mode (artist-painted CoC override) is requested | collaborative |
| D025 | M009-mso5fb | convention | License compatibility for opendefocus integration | EUPL-1.2 fork linked into GPL-3.0 DeepC project (compatible via EUPL Article 5) | EUPL-1.2 explicitly lists GPL-3.0 as a compatible license in Article 5. The fork stays EUPL-1.2, DeepC stays GPL-3.0. Attribution in THIRD_PARTY_LICENSES.md is required. | No | collaborative |
| D026 | M009-mso5fb/S03 planning | arch | Holdout attenuation implementation layer for DeepCOpenDefocus | C++ post-defocus in renderStripe — fetch holdout DeepPlane after FFI call returns, compute transmittance product, multiply output_rgba in place | Implementing holdout in C++ avoids adding holdout sample data to the FFI boundary (which would require new structs in lib.rs and the C header). The post-defocus timing is semantically correct: holdout provides sharp depth-aware occlusion, so it is applied after the blurred result is computed. This matches the described behavior in the milestone context. The holdout does not need to be in Rust because it requires no GPU convolution — only a per-pixel transmittance product. | Yes — if holdout needs depth-sorted interaction with defocus layers, move to Rust FFI | agent |
| D027 | M009-mso5fb/S01 planning | arch | FFI implementation approach for S01 opendefocus-deep bridge | Plain `extern "C"` with `#[no_mangle]` Rust exports and a hand-written C header (`include/opendefocus_deep.h`) — not the CXX crate specified in D022 | The research doc explicitly recommends extern "C" over CXX for S01 because CXX requires CMake to also compile cxxbridge-generated .cc files and find cxxbridge include paths — adding fragile path-chasing to an already-complex CMake+cargo integration. For a static lib with a narrow FFI surface (one function, one struct), extern "C" is fully type-safe on the Rust side and simpler for CMake. The spirit of D022 (type-safe FFI, no cbindgen) is preserved. CXX remains available for S02 if needed for the GPU dispatch bridge. | Yes — S02 can migrate to CXX crate if the GPU dispatch bridge benefits from shared struct generation | agent |
| D028 | M009-mso5fb/S03/T01 | architecture | Where to implement holdout attenuation relative to the Rust FFI boundary | Entirely in C++ renderStripe post-FFI, no Rust changes | The holdout logic (DeepOp input, transmittance accumulation, output multiply) requires only C++ DDImage types. Adding it to the Rust FFI would require new buffer parameters and a more complex interface with no benefit. Post-FFI placement keeps the Rust library clean and generic. | Yes | agent |
