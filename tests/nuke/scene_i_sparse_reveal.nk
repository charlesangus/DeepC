version 17.0 v3
Root {
 inputs 0
 name scene_i_sparse_reveal.nk
 last_frame 1
 format "256 256 0 0 256 256 1 deepc_val_256"
 proxy_type scale
 proxy_format "1024 778 0 0 1024 778 1 1K_Super_35(full-ap)"
 colorManagement Nuke
 workingSpaceLUT linear
 monitorLut sRGB
 monitorOutLUT rec709
 int8Lut sRGB
 int16Lut sRGB
 logLut Cineon
 floatLut linear
}
Constant {
 inputs 0
 color {0 0 0 0}
 format "256 256 0 0 256 256 1 deepc_val_256"
 name Constant1
}
Expression {
 expr0 "(x>=64 && x<192 && y>=64 && y<192) ? 0.8 : 0.0"
 expr1 0.0
 expr2 "(x>=64 && x<192 && y>=64 && y<192) ? 0.0 : 0.6"
 expr3 1.0
 name Expression1
 xpos -160
}
Expression {
 channel0 depth
 expr0 "1.0/((x>=64 && x<192 && y>=64 && y<192) ? 4 : 20)"
 name Expression2
}
DeepFromImage {
 name DeepFromImage1
 label "sparse: one sample per pixel, no hidden data"
 xpos -160
 ypos 60
}
DeepCDefocus {
 coc_mode Manual
 focus_distance 20
 size 6
 name DeepCDefocus1
 label "must show the honest alpha dip"
 xpos -160
 ypos 180
}
Constant {
 inputs 0
 color {0 0 0.6 1}
 format "256 256 0 0 256 256 1 deepc_val_256"
 name Constant3
}
DeepFromImage {
 premult true
 set_z true
 z 20
 name DeepFromImage3
 xpos 220
}
Constant {
 inputs 0
 color {0 0 0 0}
 format "256 256 0 0 256 256 1 deepc_val_256"
 name Constant2
}
Rectangle {
 area {64 64 192 192}
 color {0.8 0 0 1}
 name Rectangle1
}
DeepFromImage {
 premult true
 set_z true
 z 4
 name DeepFromImage2
 xpos 60
}
DeepMerge2 {
 inputs 2
 name DeepMerge1
 label "DeepMerge twin: same visible content, occluded far samples KEPT"
 xpos 140
 ypos 60
}
DeepCDefocus {
 coc_mode Manual
 focus_distance 20
 size 6
 name DeepCDefocus2
 label "must NOT dip (hidden samples present)"
 xpos 140
 ypos 180
}
StickyNote {
 inputs 0
 name StickyNote1
 label "Scene (i) -- sparse reveal / coverage deficit\n\nCheck: an opaque near card over a distant card, built with exactly one sample per pixel (no hidden data anywhere), must show a documented alpha dip inside the silhouette when the near card is defocused -- and no fabricated colour behind that dip.\n\nTwo branches: 'sparse' (one sample per pixel; must dip) and 'DeepMerge twin' (the same visible content but with the occluded far card's samples kept; must NOT dip). Agreement of the two confirms the dip is the missing hidden data, not a node artefact. The harness's pre_merge reachability sweep (i6/i7) is not reproduced here."
 note_font_size 14
 xpos 340
}
